// file: frontend/src/components/Loading.jsx

/**
 * Loading Component - spinner
 *
 * Vai trò: UI thuần, hiển thị loading state
 */

export default function Loading({ message = "Đang tải..." }) {
  return (
    <div className="flex flex-col items-center justify-center py-20">
      <div className="w-12 h-12 border-4 border-gray-200 border-t-gray-900 rounded-full animate-spin mb-4"></div>
      <p className="text-gray-500 text-sm font-light">{message}</p>
    </div>
  );
}
